{% extends 'base.html.twig' %}

{% block title %}{{ ressource.nom }}{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <div>
                        <h3 class="mb-0">
                            <i class="fas fa-file-alt text-secondary"></i> 
                            {{ ressource.nom }}
                        </h3>
                        <small class="text-muted">
                            Cours: <a href="{{ path('app_cours_show', {'id': ressource.cours.id}) }}">{{ ressource.cours.titre }}</a>
                        </small>
                    </div>
                    <div>
                        <span class="badge bg-secondary">{{ ressource.type ?: 'RESSOURCE' }}</span>
                        <a href="{{ path('app_cours_show', {'id': ressource.cours.id}) }}" 
                           class="btn btn-outline-secondary btn-sm ms-2">
                            <i class="fas fa-arrow-left"></i> Retour au cours
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    {% if ressource.url %}
                    <div class="text-center mb-4">
                        <div class="file-preview p-5 bg-light rounded">
                            <i class="fas fa-file-alt fa-5x text-muted mb-3"></i>
                            <h4>{{ ressource.nom }}</h4>
                            <p class="text-muted">Ressource disponible</p>
                        </div>
                    </div>
                    {% endif %}

                    {% if ressource.contenu %}
                    <div class="mt-4">
                        <h5>Contenu</h5>
                        <div class="p-3 bg-light rounded">
                            {{ ressource.contenu|nl2br }}
                        </div>
                    </div>
                    {% endif %}
                </div>
                <div class="card-footer">
                    <div class="row">
                        <div class="col-md-6">
                            <small class="text-muted">
                                <i class="fas fa-calendar"></i> 
                                Ajoutée le {{ ressource.dateDeCreation ? ressource.dateDeCreation|date('d/m/Y H:i') : 'N/A' }}
                            </small>
                        </div>
                        <div class="col-md-6 text-end">
                            {% if ressource.url %}
                            <a href="{{ ressource.url }}" target="_blank" 
                               class="btn btn-outline-primary">
                                <i class="fas fa-external-link-alt"></i> Ouvrir
                            </a>
                            {% endif %}
                            
                            <a href="{{ path('app_cours_show', {'id': ressource.cours.id}) }}" 
                               class="btn btn-outline-secondary ms-2">
                                <i class="fas fa-arrow-left"></i> Retour au cours
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
