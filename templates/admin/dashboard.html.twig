{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard | CreaCo{% endblock %}

{% block stylesheets %}
<style>
    .stat-card {
        transition: transform 0.3s ease;
    }
    .stat-card:hover {
        transform: translateY(-5px);
    }
    /* AI Bot Custom Styling */
    .glass-chat {
        background: rgba(255, 255, 255, 0.85);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
    }
    .custom-scrollbar::-webkit-scrollbar {
        width: 5px;
    }
    .custom-scrollbar::-webkit-scrollbar-track {
        background: transparent;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: #e2e8f0;
        border-radius: 10px;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb:hover {
        background: #cbd5e1;
    }
    .pulse-dot {
        width: 8px;
        height: 8px;
        background-color: #10b981;
        border-radius: 50%;
        display: inline-block;
        margin-right: 4px;
        box-shadow: 0 0 0 0 rgba(16, 185, 129, 1);
        transform: scale(1);
        animation: pulse 2s infinite;
    }
    @keyframes pulse {
        0% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.7); }
        70% { transform: scale(1); box-shadow: 0 0 0 10px rgba(16, 185, 129, 0); }
        100% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(16, 185, 129, 0); }
    }
    .chip {
        cursor: pointer;
        transition: all 0.2s ease;
    }
    .chip:hover {
        transform: scale(1.05);
        background: #4a5568 !important;
        color: white !important;
    }
    /* User Requested Styles */
    .ai-assistant-container {
        bottom: 30px !important;
        right: 30px !important;
        left: auto !important;
        position: fixed !important;
    }
    div#chat-window {
        border-radius: 20px;
        bottom: calc(100% + 16px) !important;
        top: auto !important;
        left: auto !important;
        right: 0;
        width: 380px;
        height: 400px;
        max-width: 90vw !important; /* ✅ Mobile safety */
        /* Note: Opacity is controlled by JS toggle, but we set base here */
    }
</style>
{% endblock %}

{% block body %}
<!-- 🚀 Statistics Row -->
<div class="flex flex-wrap -mx-3">
    <!-- Ideas Column -->
    <div class="w-full max-w-full px-3 mb-6 sm:w-1/2 sm:flex-none xl:mb-0 xl:w-1/4">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border stat-card">
            <div class="flex-auto p-4">
                <div class="flex flex-row -mx-3">
                    <div class="flex-none w-2/3 max-w-full px-3">
                        <div>
                            <p class="mb-0 font-sans font-semibold leading-normal text-sm">Flow Ideas</p>
                            <h5 class="mb-0 font-bold">
                                {{ workflowStats.ideas.total }}
                                <span class="leading-normal text-sm font-weight-bolder text-fuchsia-500">+{{ workflowStats.ideas.today }} today</span>
                            </h5>
                            <p class="mb-0 text-xs text-slate-400">Month: {{ workflowStats.ideas.month }} | Year: {{ workflowStats.ideas.year }}</p>
                        </div>
                    </div>
                    <div class="px-3 text-right basis-1/3">
                        <div class="inline-block w-12 h-12 text-center rounded-lg bg-gradient-to-tl from-purple-700 to-pink-500">
                            <i class="ni ni-bulb-61 text-lg relative top-3.5 text-white"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Missions Column -->
    <div class="w-full max-w-full px-3 mb-6 sm:w-1/2 sm:flex-none xl:mb-0 xl:w-1/4">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border stat-card">
            <div class="flex-auto p-4">
                <div class="flex flex-row -mx-3">
                    <div class="flex-none w-2/3 max-w-full px-3">
                        <div>
                            <p class="mb-0 font-sans font-semibold leading-normal text-sm">Active Missions</p>
                            <h5 class="mb-0 font-bold">
                                {{ workflowStats.missions.total }}
                                <span class="leading-normal text-sm font-weight-bolder text-fuchsia-500">+{{ workflowStats.missions.today }} today</span>
                            </h5>
                            <p class="mb-0 text-xs text-slate-400">Month: {{ workflowStats.missions.month }} | Year: {{ workflowStats.missions.year }}</p>
                        </div>
                    </div>
                    <div class="px-3 text-right basis-1/3">
                        <div class="inline-block w-12 h-12 text-center rounded-lg bg-gradient-to-tl from-purple-700 to-pink-500">
                            <i class="ni ni-briefcase-24 text-lg relative top-3.5 text-white"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Tasks Column -->
    <div class="w-full max-w-full px-3 mb-6 sm:w-1/2 sm:flex-none xl:mb-0 xl:w-1/4">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border stat-card">
            <div class="flex-auto p-4">
                <div class="flex flex-row -mx-3">
                    <div class="flex-none w-2/3 max-w-full px-3">
                        <div>
                            <p class="mb-0 font-sans font-semibold leading-normal text-sm">Tasks Tracked</p>
                            <h5 class="mb-0 font-bold">
                                {{ workflowStats.tasks.total }}
                                <span class="leading-normal text-sm font-weight-bolder text-fuchsia-500">+{{ workflowStats.tasks.today }} today</span>
                            </h5>
                            <p class="mb-0 text-xs text-slate-400">Month: {{ workflowStats.tasks.month }} | Year: {{ workflowStats.tasks.year }}</p>
                        </div>
                    </div>
                    <div class="px-3 text-right basis-1/3">
                        <div class="inline-block w-12 h-12 text-center rounded-lg bg-gradient-to-tl from-purple-700 to-pink-500">
                            <i class="ni ni-check-bold text-lg relative top-3.5 text-white"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Events Column -->
    <div class="w-full max-w-full px-3 sm:w-1/2 sm:flex-none xl:w-1/4">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border stat-card">
            <div class="flex-auto p-4">
                <div class="flex flex-row -mx-3">
                    <div class="flex-none w-2/3 max-w-full px-3">
                        <div>
                            <p class="mb-0 font-sans font-semibold leading-normal text-sm">Total Events</p>
                            <h5 class="mb-0 font-bold">
                                {{ eventStats.total }}
                                <span class="leading-normal text-sm font-weight-bolder text-fuchsia-500">+{{ eventStats.today }} today</span>
                            </h5>
                            <p class="mb-0 text-xs text-slate-400">Month: {{ eventStats.month }} | Year: {{ eventStats.year }}</p>
                        </div>
                    </div>
                    <div class="px-3 text-right basis-1/3">
                        <div class="inline-block w-12 h-12 text-center rounded-lg bg-gradient-to-tl from-purple-700 to-pink-500">
                            <i class="ni ni-calendar-grid-58 text-lg relative top-3.5 text-white"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="flex flex-wrap mt-6 -mx-3">
    <!-- 🏢 Recent Accounts Card -->
    <div class="max-w-full w-1/2 px-3 mb-6 xl:w-8/12">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border">
            <div class="p-4 pb-0 mb-0 bg-white rounded-t-2xl">
                <div class="flex justify-between">
                    <h6 class="mb-2">Last 10 Created Accounts</h6>
                    <a href="{{ path('app_admin') }}" class="text-sm m-2 font-bold text-fuchsia-500 uppercase">View All</a>
                </div>
            </div>
            <div class="table-responsive">
                <table class="items-center w-full mb-0 align-top border-gray-200 text-slate-500">
                    <thead class="align-bottom">
                        <tr>
                            <th class="px-6 py-3 font-bold text-left uppercase align-middle bg-transparent border-b border-gray-200 shadow-none text-xxs border-b-solid tracking-tight-soft opacity-70">User</th>
                            <th class="px-6 py-3 pl-2 font-bold text-left uppercase align-middle bg-transparent border-b border-gray-200 shadow-none text-xxs border-b-solid tracking-tight-soft opacity-70">Role</th>                        </tr>
                    </thead>
                    <tbody>
                        {% for user in recentUsers %}
                        <tr>
                            <td class="p-2 align-middle bg-transparent border-b whitespace-nowrap shadow-transparent">
                                <div class="flex px-4 py-1">
                                    <div class="flex flex-col justify-center">
                                        <h6 class="mb-0 text-sm leading-normal">{{ user.username }}</h6>
                                        <p class="mb-0 text-xs leading-tight text-slate-400">{{ user.email }}</p>
                                    </div>
                                </div>
                            </td>
                            <td class="p-2 align-middle bg-transparent border-b whitespace-nowrap shadow-transparent">
                                {% if user.role == 'ROLE_ADMIN' %}
										<span class="py-2 px-2 text-xs rounded-1.8 inline-block whitespace-nowrap text-center bg-gradient-to-tl from-gray-900 to-slate-800 align-baseline font-bold uppercase leading-none text-white">Admin</span>
                                {% elseif user.role == 'ROLE_MANAGER' %}
										<span class="py-2 px-2 text-xs rounded-1.8 inline-block whitespace-nowrap text-center bg-gradient-to-tl from-green-600 to-lime-400 align-baseline font-bold uppercase leading-none text-white">Manager</span>
                                {% elseif user.role == 'ROLE_CONTENT_CREATOR' %}
										<span class="py-2 px-2 text-xs rounded-1.8 inline-block whitespace-nowrap text-center bg-gradient-to-tl from-purple-700 to-pink-500 align-baseline font-bold uppercase leading-none text-white">Content Creator</span>
                                {% elseif user.role == 'ROLE_MEMBER' %}
										<span class="py-2 px-2 text-xs rounded-1.8 inline-block whitespace-nowrap text-center bg-gradient-to-tl from-blue-600 to-cyan-400 align-baseline font-bold uppercase leading-none text-white">Member</span>
                                {% else %}
                                    <span class="px-2.5 py-1 text-sm font-bold text-white uppercase rounded-lg bg-gradient-to-tl from-gray-600 to-gray-400">{{ user.role|replace({'ROLE_': ''}) }}</span>
                                {% endif %}
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- 📊 Activity Summary -->
    <div class="max-w-full w-1/2 px-3 xl:w-8/12">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border mb-6">
            <div class="p-4 pb-0 mb-0 bg-white rounded-t-2xl">
                <h6 class="mb-0">Forum & Courses Summary</h6>
            </div>
            <div class="flex-auto p-4">
                <ul class="flex flex-col pl-0 mb-0 rounded-lg">
                    <li class="relative flex justify-between py-2 pr-4 mb-2 bg-white border-0 rounded-t-lg text-inherit">
                        <div class="flex items-center">
                            <div class="inline-block w-8 h-8 mr-4 text-center rounded-lg bg-gradient-to-tl from-gray-900 to-slate-800">
                                <i class="ni ni-chat-round text-white relative top-2 text-xs"></i>
                            </div>
                            <div class="flex flex-col">
                                <h6 class="mb-1 text-sm leading-normal text-slate-700">Forum Activity</h6>
                                <span class="text-xs leading-tight">{{ forumStats.posts.total }} Posts | {{ forumStats.comments.total }} Replies</span>
                            </div>
                        </div>
                        <div class="flex items-center text-sm leading-normal">
                             <span class="font-bold text-green-500">+{{ forumStats.posts.today + forumStats.comments.today }}</span>
                        </div>
                    </li>
                    <li class="relative flex justify-between py-2 pr-4 mb-2 bg-white border-0 text-inherit">
                        <div class="flex items-center">
                            <div class="inline-block w-8 h-8 mr-4 text-center rounded-lg bg-gradient-to-tl from-gray-900 to-slate-800">
                                <i class="ni ni-hat-3 text-white relative top-2 text-xs"></i>
                            </div>
                            <div class="flex flex-col">
                                <h6 class="mb-1 text-sm leading-normal text-slate-700">Course Content</h6>
                                <span class="text-xs leading-tight">{{ courseStats.total }} Courses Added</span>
                            </div>
                        </div>
                        <div class="flex items-center text-sm leading-normal">
                            <span class="font-bold text-green-500">+{{ courseStats.today }}</span>
                        </div>
                    </li>
                </ul>

                <hr class="h-px mt-4 mb-6 bg-transparent bg-gradient-to-r from-transparent via-black/40 to-transparent" />

                <h6 class="mb-4 text-xs font-bold leading-tight uppercase opacity-60">Top Courses by Views</h6>
                <div class="flex flex-col">
                    {% for course in topCourses %}
                    <div class="flex items-center mb-4">
                        <div class="flex-auto pl-2">
                            <h6 class="mb-0 text-sm font-semibold text-slate-700">{{ course.titre }}</h6>
                            <div class="w-full bg-gray-200 rounded-lg h-1.5 mt-1">
                                <div class="bg-gradient-to-tl from-blue-600 to-cyan-400 h-1.5 rounded-lg" style="width: {{ (course.views / topCourses[0].views * 100)|default(0) }}%"></div>
                            </div>
                        </div>
                        <span class="ml-auto text-sm font-bold text-slate-700">{{ course.views }}</span>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>

<!-- 📈 Forum Activity Graph -->
<div class="flex flex-wrap mt-6 -mx-3">
    <div class="w-full max-w-full px-3">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border">
            <div class="p-4 pb-0 mb-0 bg-white rounded-t-2xl">
                <h6 class="mb-0 font-bold">Forum Engagement (Last 7 Days)</h6>
                <p class="text-sm leading-normal">Daily tracking of posts and replies</p>
            </div>
            <div class="flex-auto p-4">
                <div class="h-80">
                    <canvas id="chart-forum" class="chart-canvas" height="300"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- 📈 Charts Row -->
<div class="flex flex-wrap mt-6 -mx-3">
    <div class="w-full max-w-full px-3 mb-6 lg:w-7/12">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border">
            <div class="flex-auto p-4">
                <h6 class="mb-0 font-bold">Activity Overview</h6>
                <p class="leading-normal text-sm">Last period comparisons</p>
                <div class="h-80">
                    <canvas id="chart-bars" class="chart-canvas" height="300"></canvas>
                </div>
            </div>
        </div>
    </div>
    <div class="w-full max-w-full px-3 lg:w-5/12">
        <div class="relative flex flex-col min-w-0 break-words bg-white shadow-soft-xl rounded-2xl bg-clip-border">
            <div class="p-4 pb-0 mb-0 bg-white rounded-t-2xl">
                <h6 class="mb-0 text-fuchsia-500 font-bold">Workflow Health</h6>
            </div>
            <div class="flex-auto p-4">
                <div class="h-80">
                    <canvas id="chart-line" class="chart-canvas" height="300"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block javascripts %}
{{ parent() }}
<div id="ai-assistant-wrapper">
    <!-- 🤖 AI Assistant Bot -->
    <div class="fixed z-[9999] ai-assistant-container">
        <!-- Chat Icon -->
        <button id="chat-bot-toggle" class="group w-12 h-12 rounded-full bg-gradient-to-tl from-purple-700 to-pink-500 shadow-soft-2xl flex items-center justify-center text-white text-2xl hover:scale-110 active:scale-95 transition-all duration-300 focus:outline-none ring-4 ring-white/20">
            <i class="fas fa-robot group-hover:rotate-12 transition-transform" id="chat-icon"></i>
            <i class="fas fa-times hidden" id="close-icon"></i>
        </button>

        <!-- Chat Window -->
        <div id="chat-window" class="absolute glass-chat rounded-3xl shadow-soft-2xl border border-white/40 flex flex-col scale-0 opacity-0 origin-bottom-right transition-all duration-500 ease-out pointer-events-none overflow-hidden" style="bottom: calc(100% + 16px); right: 0; width: 380px; height: 500px; max-width: 90vw;">
            <!-- Header -->
            <div class="p-5 bg-gradient-to-tl from-purple-700 to-pink-500 flex items-center justify-between">
                <div class="flex items-center">
                    <div class="w-12 h-12 rounded-2xl bg-white/20 flex items-center justify-center mr-4 shadow-inner">
                        <i class="fas fa-robot text-white text-xl"></i>
                    </div>
                    <div>
                        <h6 class="text-white mb-0 font-bold tracking-tight">System Assistant</h6>
                        <p class="text-white/80 text-xs mb-0 flex items-center">
                            <span class="pulse-dot"></span> Online Intelligence
                        </p>
                    </div>
                </div>
                <button id="clear-chat" class="mr-4 text-white/60 hover:text-white transition-colors">
                    <i class="fas fa-trash-alt text-sm"></i>
                </button>
            </div>

            <!-- Chat Area -->
            <div id="chat-messages" class="flex-auto overflow-y-auto p-2 space-y-5 custom-scrollbar bg-slate-50/30">
                <div class="flex items-start animate-fade-in">
                    <div class="w-8 h-8 rounded-full bg-purple-100 flex items-center justify-center mr-2 mt-1 flex-shrink-0">
                        <i class="fas fa-robot text-purple-600 text-xs"></i>
                    </div>
                    <div class="bg-white p-4 rounded-2xl rounded-tl-none shadow-soft-xs text-sm text-slate-700 max-w-[85%] border border-slate-100 leading-relaxed">
                        Hello <span class="font-bold text-purple-600">Admin</span>! I've loaded the latest system analytics. How can I assist you today?
                    </div>
                </div>
                <!-- Action Chips Container -->
                <div id="quick-actions" class="flex flex-wrap gap-2 mt-2">
                    <span class="chip bg-slate-100 text-slate-600 px-3 py-1.5 rounded-full text-xs font-medium border border-slate-200" data-query="Give me a general statistics overview">📊 Overview</span>
                    <span class="chip bg-slate-100 text-slate-600 px-3 py-1.5 rounded-full text-xs font-medium border border-slate-200" data-query="How many users and courses are there?">👥 Users & Courses</span>
                    <span class="chip bg-slate-100 text-slate-600 px-3 py-1.5 rounded-full text-xs font-medium border border-slate-200" data-query="What is the forum engagement level?">💬 Forum Health</span>
                </div>
            </div>

            <!-- Input Area -->
            <div class="p-4 bg-white/50 border-t border-white/40">
                <form id="chat-form" class="flex items-center gap-3">
                    <div class="relative flex-auto">
                        <input type="text" id="chat-input" placeholder="Ask about platform stats..." class="w-full bg-white border border-slate-200 rounded-2xl pl-4 pr-10 py-2 text-sm focus:ring-4 focus:ring-purple-500/10 focus:border-purple-500 transition-all outline-none" required>
                    </div>
                    <button type="submit" class="w-10 h-12 ml-2 rounded-xl bg-gradient-to-tl from-purple-700 to-pink-500 flex items-center justify-center text-white shadow-soft-md hover:shadow-soft-lg active:scale-95 transition-all outline-none">
                        <i class="fas fa-location-arrow rotate-45"></i>
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    document.addEventListener("DOMContentLoaded", function() {
        // Chart 1: Bar Chart for Main Entities
        var ctx = document.getElementById("chart-bars").getContext("2d");
        new Chart(ctx, {
            type: "bar",
            data: {
                labels: ["Ideas", "Missions", "Tasks", "Events", "Posts", "Courses"],
                datasets: [{
                    label: "Today's Growth",
                    tension: 0.4,
                    borderWidth: 0,
                    borderRadius: 4,
                    borderSkipped: false,
                    backgroundColor: "#cb0c9f",
                    data: [
                        {{ workflowStats.ideas.today }}, 
                        {{ workflowStats.missions.today }}, 
                        {{ workflowStats.tasks.today }}, 
                        {{ eventStats.today }}, 
                        {{ forumStats.posts.today }}, 
                        {{ courseStats.today }}
                    ],
                    maxBarThickness: 10
                },
                {
                    label: "Monthly Total",
                    tension: 0.4,
                    borderWidth: 0,
                    borderRadius: 4,
                    borderSkipped: false,
                    backgroundColor: "#3A416F",
                    data: [
                        {{ workflowStats.ideas.month }}, 
                        {{ workflowStats.missions.month }}, 
                        {{ workflowStats.tasks.month }}, 
                        {{ eventStats.month }}, 
                        {{ forumStats.posts.month }}, 
                        {{ courseStats.month }}
                    ],
                    maxBarThickness: 10
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: true,
                        position: 'top',
                    }
                },
                interaction: {
                    intersect: false,
                    mode: 'index',
                },
                scales: {
                    y: {
                        grid: {
                            drawBorder: false,
                            display: true,
                            drawOnChartArea: true,
                            drawTicks: false,
                            borderDash: [5, 5]
                        },
                        ticks: {
                            display: true,
                            padding: 10,
                            color: '#b2b9bf',
                            font: {
                                size: 11,
                                family: "Open Sans",
                                style: 'normal',
                                lineHeight: 2
                            },
                        }
                    },
                    x: {
                        grid: {
                            drawBorder: false,
                            display: false,
                            drawOnChartArea: false,
                            drawTicks: false
                        },
                        ticks: {
                            display: true,
                            color: '#b2b9bf',
                            padding: 20,
                            font: {
                                size: 11,
                                family: "Open Sans",
                                style: 'normal',
                                lineHeight: 2
                            },
                        }
                    },
                },
            },
        });

        // Chart 2: Workflow Health - Enhanced
        var ctx2 = document.getElementById("chart-line").getContext("2d");

        // Create gradients for each dataset
        var gradIdeas = ctx2.createLinearGradient(0, 0, 0, 300);
        gradIdeas.addColorStop(0, "rgba(203, 12, 159, 0.5)");
        gradIdeas.addColorStop(1, "rgba(203, 12, 159, 0.0)");

        var gradMissions = ctx2.createLinearGradient(0, 0, 0, 300);
        gradMissions.addColorStop(0, "rgba(58, 65, 111, 0.5)");
        gradMissions.addColorStop(1, "rgba(58, 65, 111, 0.0)");

        var gradTasks = ctx2.createLinearGradient(0, 0, 0, 300);
        gradTasks.addColorStop(0, "rgba(17, 205, 239, 0.5)");
        gradTasks.addColorStop(1, "rgba(17, 205, 239, 0.0)");

        new Chart(ctx2, {
            type: "line",
            data: {
                labels: ["Today", "This Month", "This Year"],
                datasets: [
                {
                    label: "💡 Ideas",
                    borderColor: "#cb0c9f",
                    backgroundColor: gradIdeas,
                    pointBackgroundColor: "#cb0c9f",
                    pointBorderColor: "#fff",
                    pointBorderWidth: 2,
                    pointRadius: 6,
                    pointHoverRadius: 9,
                    tension: 0.4,
                    borderWidth: 3,
                    fill: true,
                    data: [{{ workflowStats.ideas.today }}, {{ workflowStats.ideas.month }}, {{ workflowStats.ideas.year }}],
                },
                {
                    label: "🎯 Missions",
                    borderColor: "#3A416F",
                    backgroundColor: gradMissions,
                    pointBackgroundColor: "#3A416F",
                    pointBorderColor: "#fff",
                    pointBorderWidth: 2,
                    pointRadius: 6,
                    pointHoverRadius: 9,
                    tension: 0.4,
                    borderWidth: 3,
                    fill: true,
                    data: [{{ workflowStats.missions.today }}, {{ workflowStats.missions.month }}, {{ workflowStats.missions.year }}],
                },
                {
                    label: "✅ Tasks",
                    borderColor: "#11cdef",
                    backgroundColor: gradTasks,
                    pointBackgroundColor: "#11cdef",
                    pointBorderColor: "#fff",
                    pointBorderWidth: 2,
                    pointRadius: 6,
                    pointHoverRadius: 9,
                    tension: 0.4,
                    borderWidth: 3,
                    fill: true,
                    data: [{{ workflowStats.tasks.today }}, {{ workflowStats.tasks.month }}, {{ workflowStats.tasks.year }}],
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                interaction: {
                    mode: 'index',
                    intersect: false,
                },
                plugins: {
                    legend: {
                        display: true,
                        position: 'bottom',
                        labels: {
                            usePointStyle: true,
                            pointStyle: 'circle',
                            padding: 16,
                            font: {
                                size: 11,
                                family: "Open Sans",
                            },
                            color: '#8392ab',
                        }
                    },
                    tooltip: {
                        backgroundColor: '#1a1f3c',
                        titleColor: '#fff',
                        bodyColor: '#b2b9bf',
                        borderColor: 'rgba(255,255,255,0.1)',
                        borderWidth: 1,
                        padding: 12,
                        cornerRadius: 10,
                        displayColors: true,
                        boxWidth: 8,
                        boxHeight: 8,
                        usePointStyle: true,
                        callbacks: {
                            title: function(items) {
                                return '📅 ' + items[0].label;
                            },
                            label: function(item) {
                                return '  ' + item.dataset.label + ': ' + item.parsed.y;
                            }
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        grid: {
                            drawBorder: false,
                            display: true,
                            drawOnChartArea: true,
                            drawTicks: false,
                            color: 'rgba(130,148,175,0.1)',
                            borderDash: [4, 4],
                        },
                        ticks: {
                            display: true,
                            padding: 12,
                            color: '#8392ab',
                            precision: 0,
                            font: {
                                size: 11,
                                family: "Open Sans",
                            },
                        }
                    },
                    x: {
                        grid: {
                            drawBorder: false,
                            display: false,
                            drawOnChartArea: false,
                            drawTicks: false,
                        },
                        ticks: {
                            display: true,
                            color: '#8392ab',
                            padding: 10,
                            font: {
                                size: 11,
                                family: "Open Sans",
                                weight: '600',
                            },
                        }
                    }
                }
            }
        });

        // Chart 3: Forum Activity Area Chart
        var ctx3 = document.getElementById("chart-forum").getContext("2d");
        new Chart(ctx3, {
            type: "line",
            data: {
                labels: {{ forumHistoricalLabels|raw }},
                datasets: [{
                    label: "Forum Posts",
                    tension: 0.4,
                    borderWidth: 3,
                    pointRadius: 2,
                    borderColor: "#cb0c9f",
                    backgroundColor: "rgba(203, 12, 159, 0.2)",
                    fill: true,
                    data: {{ forumHistoricalPosts|raw }},
                    maxBarThickness: 6
                },
                {
                    label: "Comments & Replies",
                    tension: 0.4,
                    borderWidth: 3,
                    pointRadius: 2,
                    borderColor: "#3A416F",
                    backgroundColor: "rgba(58, 65, 111, 0.2)",
                    fill: true,
                    data: {{ forumHistoricalComments|raw }},
                    maxBarThickness: 6
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: true,
                        position: 'top',
                    }
                },
                interaction: {
                    intersect: false,
                    mode: 'index',
                },
                scales: {
                    y: {
                        grid: {
                            drawBorder: false,
                            display: true,
                            drawOnChartArea: true,
                            drawTicks: false,
                            borderDash: [5, 5]
                        },
                        ticks: {
                            display: true,
                            padding: 10,
                            color: '#b2b9bf',
                            font: {
                                size: 11,
                                family: "Open Sans",
                                style: 'normal',
                                lineHeight: 2
                            },
                        }
                    },
                    x: {
                        grid: {
                            drawBorder: false,
                            display: false,
                            drawOnChartArea: false,
                            drawTicks: false
                        },
                        ticks: {
                            display: true,
                            color: '#b2b9bf',
                            padding: 20,
                            font: {
                                size: 11,
                                family: "Open Sans",
                                style: 'normal',
                                lineHeight: 2
                            },
                        }
                    },
                },
            },
        });
    });

    // 🤖 AI Assistant Logic
    document.addEventListener('DOMContentLoaded', function() {
        const chatToggle = document.getElementById('chat-bot-toggle');
        const chatWindow = document.getElementById('chat-window');
        const chatIcon = document.getElementById('chat-icon');
        const closeIcon = document.getElementById('close-icon');
        const chatForm = document.getElementById('chat-form');
        const chatInput = document.getElementById('chat-input');
        const chatMessages = document.getElementById('chat-messages');
        const clearBtn = document.getElementById('clear-chat');
        const chips = document.querySelectorAll('.chip');

        let isOpen = false;

        chatToggle.addEventListener('click', () => {
            isOpen = !isOpen;
            if (isOpen) {
                chatWindow.classList.remove('scale-0', 'opacity-0', 'pointer-events-none');
                chatWindow.classList.add('scale-100', 'opacity-100', 'pointer-events-auto');
                chatIcon.classList.add('hidden');
                closeIcon.classList.remove('hidden');
                setTimeout(() => chatInput.focus(), 300);
            } else {
                chatWindow.classList.add('scale-0', 'opacity-0', 'pointer-events-none');
                chatWindow.classList.remove('scale-100', 'opacity-100', 'pointer-events-auto');
                chatIcon.classList.remove('hidden');
                closeIcon.classList.add('hidden');
            }
        });

        clearBtn.addEventListener('click', () => {
             chatMessages.innerHTML = `
                <div class="flex items-start animate-fade-in">
                    <div class="w-8 h-8 rounded-full bg-purple-100 flex items-center justify-center mr-2 mt-1">
                        <i class="fas fa-robot text-purple-600 text-xs"></i>
                    </div>
                    <div class="bg-white p-4 rounded-2xl rounded-tl-none shadow-soft-xs text-sm text-slate-700 max-w-[85%] border border-slate-100 leading-relaxed">
                        Chat history cleared. How can I help you now?
                    </div>
                </div>
             `;
        });

        chips.forEach(chip => {
            chip.addEventListener('click', () => {
                sendMessage(chip.dataset.query);
            });
        });

        chatForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const message = chatInput.value.trim();
            if (message) sendMessage(message);
        });

        async function sendMessage(message) {
            // Add user message
            addMessage(message, 'user');
            chatInput.value = '';

            // Add loading message
            const loadingId = 'loading-' + Date.now();
            addLoadingMessage(loadingId);

            try {
                const response = await fetch('{{ path('admin_ai_chat') }}', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ message })
                });

                const data = await response.json();
                removeLoadingMessage(loadingId);

                if (data.reply) {
                    addMessage(data.reply, 'bot');
                } else {
                    addMessage('Sorry, something went wrong. ' + (data.error || 'System error.'), 'bot');
                }
            } catch (error) {
                removeLoadingMessage(loadingId);
                addMessage('Communication failure with the API.', 'bot');
            }
        }

        function formatMarkdown(text) {
            // Very simple markdown-lite
            return text
                .replace(/\n/g, '<br>')
                .replace(/\*\*(.*?)\*\*/g, '<span class="font-bold text-slate-800">$1</span>')
                .replace(/\*(.*?)\*/g, '<em class="italic">$1</em>')
                .replace(/- (.*?)($|<br>)/g, '<li class="ml-4">$1</li>');
        }

        function addMessage(text, sender) {
            const div = document.createElement('div');
            div.className = sender === 'user' 
                ? 'flex items-start justify-end mt-4' 
                : 'flex items-start mt-4 animate-fade-in';
            
            const timestamp = new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
            
            if (sender === 'user') {
                div.innerHTML = `
                    <div class="flex flex-col items-end max-w-[85%]">
                        <div class="bg-gradient-to-tl from-purple-700 to-pink-500 text-white p-4 rounded-3xl rounded-tr-none shadow-soft-md text-sm leading-relaxed">
                            ${text}
                        </div>
                        <span class="text-[10px] text-slate-400 mt-1 mr-1">${timestamp}</span>
                    </div>
                `;
            } else {
                div.innerHTML = `
                    <div class="w-8 h-8 rounded-full bg-purple-100 flex items-center justify-center mr-2 mt-1 flex-shrink-0">
                        <i class="fas fa-robot text-purple-600 text-xs"></i>
                    </div>
                    <div class="flex flex-col max-w-[85%]">
                        <div class="bg-white p-4 rounded-3xl rounded-tl-none shadow-soft-xs text-sm text-slate-700 border border-slate-100 leading-relaxed">
                            ${formatMarkdown(text)}
                        </div>
                        <span class="text-[10px] text-slate-400 mt-1 ml-1">${timestamp}</span>
                    </div>
                `;
            }

            chatMessages.appendChild(div);
            chatMessages.scrollTo({ top: chatMessages.scrollHeight, behavior: 'smooth' });
        }

        function addLoadingMessage(id) {
            const div = document.createElement('div');
            div.id = id;
            div.className = 'flex items-start mt-4';
            div.innerHTML = `
                <div class="w-8 h-8 rounded-full bg-slate-100 flex items-center justify-center mr-2 mt-1">
                    <i class="fas fa-robot text-slate-400 text-xs"></i>
                </div>
                <div class="bg-white/50 p-4 rounded-3xl rounded-tl-none border border-slate-100 h-10 flex items-center">
                    <div class="flex space-x-1.5">
                        <div class="w-1.5 h-1.5 bg-slate-300 rounded-full animate-bounce"></div>
                        <div class="w-1.5 h-1.5 bg-slate-400 rounded-full animate-bounce" style="animation-delay: 0.2s"></div>
                        <div class="w-1.5 h-1.5 bg-slate-300 rounded-full animate-bounce" style="animation-delay: 0.4s"></div>
                    </div>
                </div>
            `;
            chatMessages.appendChild(div);
            chatMessages.scrollTo({ top: chatMessages.scrollHeight, behavior: 'smooth' });
        }

        function removeLoadingMessage(id) {
            const element = document.getElementById(id);
            if (element) element.remove();
        }
    });
</script>
{% endblock %}
