{% extends './base.html.twig' %}

{% block title %}Course category
{% endblock %}

{% block body %}
	<h1>Course category</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ categorie_cours.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ categorie_cours.nom }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ categorie_cours.description }}</td>
			</tr>
			<tr>
				<th>Created at</th>
				<td>{{ categorie_cours.dateDeCreation ? categorie_cours.dateDeCreation|date('d/m/Y H:i') : '-' }}</td>
			</tr>
			<tr>
				<th>Updated at</th>
				<td>{{ categorie_cours.dateDeModification ? categorie_cours.dateDeModification|date('d/m/Y H:i') : '-' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_categorie_cours_index') }}">back to list</a>

	<a href="{{ path('app_categorie_cours_edit', {'id': categorie_cours.id}) }}">edit</a>

	{{ include('back/categorie_cours/_delete_form.html.twig') }}
{% endblock %}
