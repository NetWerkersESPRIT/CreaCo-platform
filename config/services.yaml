parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true
        bind:
            $apiKey: '%env(IMGBB_API_KEY)%'

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # ✅ ADD THIS
    App\Service\GoogleDriveUploader:
        arguments:
            $credentialsPath: '%env(resolve:GOOGLE_DRIVE_CREDENTIALS)%'
            $folderId: '%env(GOOGLE_DRIVE_FOLDER_ID)%'
